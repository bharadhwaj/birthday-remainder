<% provide(:title, "Login")%>

<style type="text/css">
  .navbar-nav > li{
    font-size: 14px;
  }
</style>

<div class="container">      

      <%= form_for(:session, url: login_path, html: {class: 'form-signin'}) do |f| %>
        <h2 class="form-signin-heading">Please sign in</h2>
        <%= f.email_field :email, class: 'form-control', placeholder: 'example@example.com', required: true, autofocus: true%>
        <%= f.password_field :password, class: 'form-control', required: true, placeholder: 'Password' %>

        <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
      <% end %>

      <div class = "row form-signin" style="padding-top:1em; padding-bottom:1em; font-size: 16px">
      </div>

</div>
