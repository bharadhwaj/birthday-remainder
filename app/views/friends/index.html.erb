<div class="container">
	<div class="row">
		<div class="col-md-10 col-md-offset-1">
		    <h4><a href="<%= new_friend_path %>" class="btn btn-primary">Add new birthday</a></h4>
				<center> <h3> All friends </h3> </center>
			    <table id="empTable" class="table table-hover">
			    <thead>
			                <tr>
			                  <th> # </th>
			                  <th> Name </th>
			                  <th> Email ID</th>
			                  <th> Date of Birth</th>
			                  <th> Birthday Message </th>
			                  <th></th>
			                  <th></th>
			                </tr>
			    </thead>
			    <tbody>
			    	<% @friends.each do |friend| %>
			            <tr>
			            	<td>  </td>
				            <td> <%= friend.name %> </td>
				            <td> <%= friend.email %> </td>
				            <td> <%= friend.dob %> </td>
				            <td> <%= friend.birthday_message %> </td>
				            <td>
				            	<a href="<%= edit_friend_path(friend) %>" class="btn btn-success"> Edit </a>
				            </td>
				            <td>
<a href="" class="btn btn-danger" data-toggle="modal" data-target="#delModal">Delete</a>

<div class="modal fade" id="delModal" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title" id="myModalLabel">Delete User</h4>
        <hr>
      </div>
      <div class="modal-body">
        Are you sure you want to remove user, "<%= friend.name %>"?
      </div>
      <div class="modal-footer">
      <form method="post">
        <input type="hidden" name="formtype" value="delpost">
        <button type="submit" class="btn btn-primary">Yes</button>
        <button type="button" class="btn" data-dismiss="modal">No</button>
        </form>
      </div>
    </div>
  </div>
</div>

				            	<%= link_to "Delete", friend, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
				            </td>
			            </tr>
			        <% end %>
			  	</table>




		</div>
	</div>
</div>
<script type="text/javascript">
  $(document).ready(function(){
    $('#empTable').DataTable();
});
</script>